def run(distance):
    def cross_bridge(distance):
        i = 0
        while (i < distance):
            print("Crossed step")
            i += 1
        if (distance > 5):
            print("The bridge is collapsing!")
        else:
            print("The bridge is collapsing!")
    cross_bridge(distance)